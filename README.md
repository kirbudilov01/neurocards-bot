# Neurocards Bot

Telegram-бот для генерации вертикальных видео (Reels/TikTok) из фото товаров с помощью AI.

## 🚀 Возможности

- 📸 Генерация видео из фото товара
- 🎬 Несколько шаблонов (UGC блогер, реклама, креатив)
- 🤖 AI-генерация промптов через OpenAI GPT
- 🎥 Генерация видео через KIE.AI (Sora-2)
- 💳 Система кредитов и балансов
- 🔄 Очередь задач с защитой от дублей
- 📊 Личный кабинет пользователя

## 🏗️ Архитектура

Проект состоит из двух сервисов:

1. **Bot (Web Service)** - обработчик webhook от Telegram
2. **Worker (Background)** - обработчик очереди генерации видео

### Технологический стек

- **Python 3.11+**
- **aiogram 3** - фреймворк для Telegram ботов
- **PostgreSQL** - база данных (локальная)
- **asyncpg** - асинхронный драйвер PostgreSQL
- **KIE.AI API** - генерация видео (Sora-2 image-to-video)
- **OpenAI API** - генерация промптов (GPT-4)
- **httpx** - асинхронные HTTP запросы

## 📋 Требования

### Обязательные переменные окружения

```bash
# Telegram
BOT_TOKEN=your_telegram_bot_token
WEBHOOK_SECRET_TOKEN=random_secret_string

# Database
DATABASE_TYPE=postgres
DATABASE_URL=postgresql://user:password@localhost:5432/neurocards

# Webhook URL (для production)
PUBLIC_BASE_URL=https://your-vps-domain.com

# AI APIs
KIE_API_KEY=your_kie_api_key
OPENAI_API_KEY=your_openai_api_key
```

Полный список см. в [.env.example](.env.example)

## 🗄️ База данных

Бот поддерживает **2 режима работы с БД** (переключение через `DATABASE_TYPE`):

### Режим 1: Локальная PostgreSQL (для self-hosting)

```bash
# .env
Используется **локальная PostgreSQL** база данных.

```bash
# .env
DATABASE_TYPE=postgres
DATABASE_URL=postgresql://botuser:password@localhost:5432/neurocards

# Автоматически настраивается скриптом deploy_to_vps.sh
```

**Преимущества:**
- ✅ Полная независимость от внешних сервисов
- ✅ Быстро (нет сетевых задержек)
- ✅ Дешево (не платишь за managed БД)
- ✅ Полный контроль

### Инициализация базы данных

База данных создается и настраивается автоматически при деплое через `deploy_to_vps.sh`.

Для ручной инициализации:
```bash
# Создание базы данных
sudo -u postgres createdb neurocards

# Создание пользователя
sudo -u postgres psql -c "CREATE USER botuser WITH PASSWORD 'your_password';"
sudo -u postgres psql -c "GRANT ALL PRIVILEGES ON DATABASE neurocards TO botuser;"

# Применение схемы (создается автоматически скриптом деплоя)
psql -U botuser -d neurocards -f /path/to/schema.sql
```

```bash
# Устанавливаем на свой сервер (Hetzner, DigitalOcean, Vultr и т.д.)
./scripts/deploy_to_vps.sh YOUR_SERVER_IP
```

**Обновление бота:**
```bash
./scripts/update_vps.sh YOUR_SERVER_IP
```

**Мониторинг:**
```bash
./scripts/monitor_vps.sh YOUR_SERVER_IP
```

**Бэкап:**
```bash
./scripts/backup_vps.sh YOUR_SERVER_IP ./backups
```

ПодробнVPS Деплой (рекомендуется)

**Автоматический деплой в один клик:**

```bash
# Устанавливаем на свой сервер (Hetzner, DigitalOcean, Vultr и т.д.)
./scripts/deploy_to_vps.sh YOUR_SERVER_IP
```

**Обновление бота:**
```bash
./scripts/update_vps.sh YOUR_SERVER_IP
```

**Мониторинг:**
```bash
./scripts/monitor_vps.sh YOUR_SERVER_IP
```

**Бэкап:**
```bash
./scripts/backup_vps.sh YOUR_SERVER_IP ./backups
```

Подробная инструкция: [SELF_HOSTING.md](SELF_HOSTING.md)

**Преимущества:**
- ✅ Полный контроль над сервером
- ✅ Экономично (VPS ~$5-10/мес)
- ✅ Быстро (все на одном сервере)
- ✅ Масштабируемость (запускай сколько угодно worker'ов)
- ✅ Независимость от внешних сервисов
```bash
curl https://your-bot-url.onrender.com/healthz
# Должен вернуть: ok
```

### Логи

Все сервисы логируют:
- ✅ Успешные операции
- ⚠️ Предупреждения
- ❌ Ошибки
- 🚀 Старт/завершение
- 💥 Критические ошибки

## 🐛 Troubleshooting

### Бот не запускается

1. Проверьте переменные окружения
2. Убедитесь что `BOT_TOKEN` корректный
3. Проверьте что `PUBLIC_BASE_URL` доступен

### Генерация не работает

1. Проверьте что Worker запущен
2. Убедитесь что `KIE_API_KEY` и `OPENAI_API_KEY` установлены
3. Проверьте баланс API ключей
4. Посмотрите логи Worker'а

### Видео получаются неправильные

- aspect_ratio исправлен на `9:16` для вертикального видео
- Проверьте что используется актуальная версия кода

Полный список исправлений: [FIXES.md](FIXES.md)

## 📚 Документация

- [DEPLOYMENT.md](DEPLOYMENT.md) - инструкция по деплою
- [FIXES.md](FIXES.md) - список всех исправлений и улучшений
- [.env.example](.vps-domainаблон переменных окружения

## 🔒 Безопасность

- Никогда не коммитьте `.env` файл
- Используйте `WEBHOOK_SECRET_TOKEN` для защиты webhook
- `SUPABASE_SERVICE_ROLE_KEY` должен быть в секрете
- Регулярно ротируйте API ключи

## 📄 Лицензия

MIT

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте [FIXES.md](FIXES.md) и [DEPLOYMENT.md](DEPLOYMENT.md)
2. Посмотрите логи в Render Dashboard
3. Проверьте таблицу `jobs` в Supabase для отладки
SELF_HOSTING.md](SELF_HOSTING.md) - инструкция по деплою на VPS
- [QUICKSTART_VPS.md](QUICKSTART_VPS.md) - быстрый старт
- Регулярно ротируйте API ключи
- Используйте firewall на VPS (настраивается автоматически)
- Регулярно обновляйте систему и пакетыle.vps) - шаблон переменных окружения для VPSSELF_HOSTING.md](SELF_HOSTING.md)
2. Посмотрите логи: `journalctl -u neurocards-bot -f`
3. Проверьте статус сервисов: `systemctl status neurocards-bot`
4. Используйте скрипт мониторинга: `./scripts/monitor_vps.sh YOUR_SERVER_IP`