# Neurocards Bot

Telegram-бот для генерации вертикальных видео (Reels/TikTok) из фото товаров с помощью AI.

## 🚀 Возможности

- 📸 Генерация видео из фото товара
- 🎬 Несколько шаблонов (UGC блогер, реклама, креатив)
- 🤖 AI-генерация промптов через OpenAI GPT
- 🎥 Генерация видео через KIE.AI (Sora-2)
- 💳 Система кредитов и балансов
- 🔄 Очередь задач с защитой от дублей
- 📊 Личный кабинет пользователя

## 🏗️ Архитектура

Проект состоит из двух сервисов:

1. **Bot (Web Service)** - обработчик webhook от Telegram
2. **Worker (Background)** - обработчик очереди генерации видео

### Технологический стек

- **Python 3.11+**
- **aiogram 3** - фреймворк для Telegram ботов
- **Supabase** - PostgreSQL база данных + хранилище файлов
- **KIE.AI API** - генерация видео (Sora-2 image-to-video)
- **OpenAI API** - генерация промптов (GPT-4)
- **httpx** - асинхронные HTTP запросы

## 📋 Требования

### Обязательные переменные окружения

```bash
# Telegram
BOT_TOKEN=your_telegram_bot_token
WEBHOOK_SECRET_TOKEN=random_secret_string

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Webhook URL (для production)
PUBLIC_BASE_URL=https://your-bot.onrender.com

# AI APIs
KIE_API_KEY=your_kie_api_key
OPENAI_API_KEY=your_openai_api_key
```

Полный список см. в [.env.example](.env.example)

## 🗄️ База данных

### Миграции

Выполните SQL миграции из `supabase/migrations/` в следующем порядке:

1. `20240722120000_atomicity_and_idempotency.sql` - атомарность и idempotency
2. `20240723120000_add_unique_index_on_tg_user_id.sql` - индексы

### Storage Buckets

Создайте в Supabase Storage:
- `inputs` - для загружаемых фото
- `outputs` - для готовых видео (опционально)

## 🚀 Деплой

### Render.com (рекомендуется)

1. Подключите GitHub репозиторий к Render
2. Render автоматически обнаружит `render.yaml`
3. Настройте переменные окружения для обоих сервисов
4. Deploy!

Подробная инструкция: [DEPLOYMENT.md](DEPLOYMENT.md)

### Локальный запуск

```bash
# Установка зависимостей
pip install -r requirements.txt

# Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env

# Запуск бота (в одном терминале)
python -m app.main

# Запуск воркера (в другом терминале)
python -m worker.worker
```

## 📊 Мониторинг

### Health Check

```bash
curl https://your-bot-url.onrender.com/healthz
# Должен вернуть: ok
```

### Логи

Все сервисы логируют:
- ✅ Успешные операции
- ⚠️ Предупреждения
- ❌ Ошибки
- 🚀 Старт/завершение
- 💥 Критические ошибки

## 🐛 Troubleshooting

### Бот не запускается

1. Проверьте переменные окружения
2. Убедитесь что `BOT_TOKEN` корректный
3. Проверьте что `PUBLIC_BASE_URL` доступен

### Генерация не работает

1. Проверьте что Worker запущен
2. Убедитесь что `KIE_API_KEY` и `OPENAI_API_KEY` установлены
3. Проверьте баланс API ключей
4. Посмотрите логи Worker'а

### Видео получаются неправильные

- aspect_ratio исправлен на `9:16` для вертикального видео
- Проверьте что используется актуальная версия кода

Полный список исправлений: [FIXES.md](FIXES.md)

## 📚 Документация

- [DEPLOYMENT.md](DEPLOYMENT.md) - инструкция по деплою
- [FIXES.md](FIXES.md) - список всех исправлений и улучшений
- [.env.example](.env.example) - шаблон переменных окружения

## 🔒 Безопасность

- Никогда не коммитьте `.env` файл
- Используйте `WEBHOOK_SECRET_TOKEN` для защиты webhook
- `SUPABASE_SERVICE_ROLE_KEY` должен быть в секрете
- Регулярно ротируйте API ключи

## 📄 Лицензия

MIT

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте [FIXES.md](FIXES.md) и [DEPLOYMENT.md](DEPLOYMENT.md)
2. Посмотрите логи в Render Dashboard
3. Проверьте таблицу `jobs` в Supabase для отладки
