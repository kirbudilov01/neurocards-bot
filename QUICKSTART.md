# 🚀 Quick Start - Деплой на Render

## 1️⃣ Подготовка

### Получите API ключи:

1. **Telegram Bot Token**
   - Напишите @BotFather в Telegram
   - `/newbot` → следуйте инструкциям
   - Сохраните токен

2. **Supabase**
   - https://supabase.com → Create new project
   - Settings → API → скопируйте:
     - URL
     - service_role key

3. **KIE.AI**
   - https://kie.ai → зарегистрируйтесь
   - Получите API key

4. **OpenAI**
   - https://platform.openai.com
   - API keys → Create new key

## 2️⃣ Настройка Supabase

1. Откройте SQL Editor в Supabase
2. Выполните миграции из `supabase/migrations/`:
   - `20240722120000_atomicity_and_idempotency.sql`
   - `20240723120000_add_unique_index_on_tg_user_id.sql`

3. Создайте Storage buckets:
   - Storage → New bucket → `inputs` (public)
   - Storage → New bucket → `outputs` (public)

## 3️⃣ Деплой на Render

1. **Зайдите на https://render.com**

2. **New → Blueprint**

3. **Подключите репозиторий:**
   - Connect GitHub
   - Выберите `kirbudilov01/neurocards-bot`

4. **Render обнаружит render.yaml и создаст 2 сервиса:**
   - `neurocards-bot` (Web Service)
   - `neurocards-worker` (Worker)

5. **Настройте переменные окружения:**

   Для **neurocards-bot** (Web):
   ```
   BOT_TOKEN=<ваш токен от BotFather>
   SUPABASE_URL=https://xxx.supabase.co
   SUPABASE_SERVICE_ROLE_KEY=<ваш ключ>
   PUBLIC_BASE_URL=https://neurocards-bot.onrender.com
   WEBHOOK_SECRET_TOKEN=<любая случайная строка>
   ```

   Для **neurocards-worker** (Worker):
   ```
   BOT_TOKEN=<тот же токен>
   SUPABASE_URL=<тот же URL>
   SUPABASE_SERVICE_ROLE_KEY=<тот же ключ>
   KIE_API_KEY=<ваш ключ от KIE.AI>
   OPENAI_API_KEY=<ваш ключ от OpenAI>
   ```

6. **Deploy!**
   - Render автоматически установит зависимости
   - Запустит оба сервиса
   - При каждом push в main будет автоматический редеплой

## 4️⃣ Проверка

1. **Проверьте логи:**
   - В Render Dashboard → выберите сервис → Logs
   - Должны увидеть: `🚀 Webhook bot started`
   - Worker: `🚀 WORKER: started main loop`

2. **Проверьте health check:**
   ```bash
   curl https://neurocards-bot.onrender.com/healthz
   # Должно вернуть: ok
   ```

3. **Протестируйте бота:**
   - Найдите бота в Telegram
   - Отправьте `/start`
   - Попробуйте сгенерировать видео

## 🐛 Если что-то не работает

### Бот не отвечает
- Проверьте, что `PUBLIC_BASE_URL` правильный
- Посмотрите логи бота в Render
- Убедитесь что webhook установлен (должно быть в логах)

### Генерация не работает
- Проверьте что Worker запущен
- Убедитесь что API ключи правильные
- Проверьте баланс KIE.AI и OpenAI
- Посмотрите логи Worker'а

### Видео квадратные
- ✅ Уже исправлено в коде (`aspect_ratio: "9:16"`)
- Убедитесь что деплоите актуальную версию

## 📊 Мониторинг

- **Health check:** `GET /healthz`
- **Логи:** Render Dashboard → Logs
- **База данных:** Supabase → Table Editor → jobs
- **Баланс API:** проверяйте регулярно KIE.AI и OpenAI

## 💡 Полезные команды

```bash
# Проверить health
curl https://neurocards-bot.onrender.com/healthz

# Посмотреть последние логи (если настроен CLI)
render logs -s neurocards-bot
render logs -s neurocards-worker

# Перезапустить сервис
# Через Render Dashboard → Manual Deploy
```

## 📚 Дополнительная информация

- [README.md](README.md) - полное описание проекта
- [DEPLOYMENT.md](DEPLOYMENT.md) - детальная инструкция
- [FIXES.md](FIXES.md) - список всех исправлений

## ✅ Checklist

- [ ] Получены все API ключи
- [ ] Выполнены миграции Supabase
- [ ] Созданы storage buckets
- [ ] Настроены переменные окружения в Render
- [ ] Оба сервиса задеплоены
- [ ] Health check возвращает `ok`
- [ ] Бот отвечает на `/start`
- [ ] Генерация работает

## 🎉 Готово!

Теперь ваш бот работает и автоматически обрабатывает генерацию видео!
