# 🎬 КАК РАБОТАЕТ СИСТЕМА - ПОЛНЫЙ ФЛОУ

## 📱 Что видит пользователь:

### 1. Начало работы
```
Юзер → /start → Бот регистрирует (или находит) пользователя в БД
```

### 2. Отправка фото
```
Юзер → 📸 Фото → Бот сохраняет photo_id в состоянии
         └→ Показывает кнопки выбора промпта
```

### 3. Выбор промпта
```
Юзер → Кнопка "Прокрути вверх" (или свой текст)
         └→ GPT генерирует финальный промпт
         └→ Показывает кнопки: 1 видео / 3 видео / 5 видео
```

### 4. Выбор количества видео (НОВОЕ!)
```
Юзер → Кнопка "3 видео" 
         └→ Проверка баланса (хватает ли 3 кредита?)
         └→ Списание 3 кредитов
         └→ Создание 3 задач в БД со статусом "queued"
         └→ Уведомление: "✅ Принял! 🎬 Генерация 3 видео запущена!"
```

## ⚙️ Что происходит на бэкенде:

### Worker процесс (20 штук работают параллельно):

```
while True:
    1. Забрать задачу из очереди (status='queued')
    2. Скачать фото из Telegram
    3. Загрузить на CDN (для KIE API)
    4. Отправить в KIE.AI: create_task_sora_i2v()
       └→ Получить task_id
    5. Обновить БД: status='processing', task_id=<id>
    
    6. Polling (каждые 30 сек, до 30 минут):
       ├─ poll_record_info(task_id)
       ├─ Если "success" → скачать видео, отправить юзеру
       ├─ Если "fail" → классифицировать ошибку:
       │   ├─ USER_VIOLATION → вернуть кредит, сообщить
       │   ├─ BILLING → вернуть кредит, написать в поддержку
       │   ├─ TEMPORARY → retry до 3 раз с задержкой
       │   ├─ RATE_LIMIT → вернуть кредит, попробовать позже
       │   └─ UNKNOWN → вернуть кредит, написать в поддержку
       └─ Если timeout → вернуть кредит, сообщить об ошибке
    
    7. После завершения → показать кнопки:
       ├─ "🔄 Сделать еще с этим товаром"
       └─ "🏠 Главное меню"
    
    sleep(5 секунд) → следующая задача
```

## 🔄 Retry логика при ошибках:

```
KIE вернул error code 500 (internal error)
    ↓
Классификатор: TEMPORARY
    ↓
Попытка 1: подождать 10 сек → повтор
    ↓
Попытка 2 (если снова ошибка): подождать 20 сек → повтор
    ↓
Попытка 3 (если снова ошибка): подождать 40 сек → повтор
    ↓
Если всё равно ошибка:
    → Вернуть кредит
    → Отправить юзеру: "❌ Произошла ошибка. Кредит вернули ✅"
```

## 💾 Структура БД (generation_jobs):

```sql
id              SERIAL PRIMARY KEY
tg_user_id      BIGINT           -- Telegram ID юзера
photo_id        TEXT             -- Telegram file_id фото
prompt          TEXT             -- Финальный промпт для генерации
status          TEXT             -- queued/processing/completed/failed
task_id         TEXT             -- ID задачи в KIE API
video_url       TEXT             -- Результат (если success)
error_message   TEXT             -- Ошибка (если fail)
attempt         INT DEFAULT 1    -- Номер попытки (для retry)
started_at      TIMESTAMP        -- Когда воркер взял в работу
finished_at     TIMESTAMP        -- Когда завершилась
created_at      TIMESTAMP        -- Когда создана задача
```

## 🔑 Ротация API ключей:

```python
# Если в .env:
KIE_API_KEY_1=sk-abc123...
KIE_API_KEY_2=sk-def456...
KIE_API_KEY_3=sk-ghi789...

# Rotator:
keys = [key1, key2, key3]
current_index = 0

def get_key():
    return keys[current_index]

def mark_failed(key):
    # Переключиться на следующий ключ
    current_index = (current_index + 1) % len(keys)
```

## 📊 Пример нагрузки:

```
1000 юзеров одновременно отправляют по 3 видео = 3000 задач

20 воркеров × 5 минут на видео = 240 видео за 5 минут
                                = ~2880 видео за час

Очередь: ~120 минут при пиковой нагрузке
→ Можно добавить еще воркеров для ускорения!
```

## 🎯 Кнопки после генерации (НОВОЕ!):

```
После получения всех 3 видео:
    ↓
Бот показывает inline-кнопки:
    ├─ "🔄 Сделать еще с этим товаром"
    │    └→ Переходим к выбору промпта (фото уже есть)
    └─ "🏠 Главное меню"
         └→ Сброс состояния, начинаем заново
```

## 🐛 Обработанные баги:

1. ✅ **Дублирование уведомлений** - теперь одно сообщение на старте
2. ✅ **refund_credit()** - исправлена сигнатура функции
3. ✅ **Даты в БД** - используем datetime объекты вместо строк
4. ✅ **Множественные генерации** - каждое видео = отдельная задача в БД

## 🚀 Готово к продакшену!
