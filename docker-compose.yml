version: '3.8'

services:
<<<<<<< HEAD
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: neurocards-postgres
    environment:
      POSTGRES_DB: neurocards
      POSTGRES_USER: ${POSTGRES_USER:-neurocards}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-neurocards}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - neurocards

  # Redis Queue
  redis:
    image: redis:7-alpine
    container_name: neurocards-redis
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - neurocards

  # Telegram Bot (Webhook Handler)
  bot:
    build:
      context: .
      dockerfile: Dockerfile.bot
    container_name: neurocards-bot
    environment:
      # Telegram
      BOT_TOKEN: ${BOT_TOKEN}
      WEBHOOK_SECRET_TOKEN: ${WEBHOOK_SECRET_TOKEN}
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL}
      
      # Database
      DATABASE_TYPE: postgres
      DATABASE_URL: postgresql://${POSTGRES_USER:-neurocards}:${POSTGRES_PASSWORD}@postgres:5432/neurocards
      
      # Redis
      REDIS_URL: redis://redis:6379/0
      
      # Storage
      STORAGE_TYPE: ${STORAGE_TYPE:-local}
      STORAGE_BASE_PATH: ${STORAGE_BASE_PATH:-/app/storage}
    volumes:
      - storage_data:/app/storage
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - neurocards
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/healthz || exit 1"]
=======
  # Телеграм бот (веб-сервис, webhook)
  bot:
    build: .
    container_name: neurocards-bot
    command: python -m app.main
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - PUBLIC_BASE_URL=${PUBLIC_BASE_URL}
      - DATABASE_URL=${DATABASE_URL}
      - WEBHOOK_SECRET_TOKEN=${WEBHOOK_SECRET_TOKEN:-random_secret_token}
    ports:
      - "10000:10000"
    network_mode: host
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:10000/healthz"]
>>>>>>> 8f6520fa9541fa7c865a7c36d6faea7967bcf8fc
      interval: 30s
      timeout: 10s
      retries: 3

<<<<<<< HEAD
  # Worker (Background Job Processor) - можно масштабировать
  worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    environment:
      # Telegram (для отправки результатов)
      BOT_TOKEN: ${BOT_TOKEN}
      
      # Database
      DATABASE_TYPE: postgres
      DATABASE_URL: postgresql://${POSTGRES_USER:-neurocards}:${POSTGRES_PASSWORD}@postgres:5432/neurocards
      
      # Redis
      REDIS_URL: redis://redis:6379/0
      
      # AI APIs (можно несколько ключей через запятую)
      KIE_API_KEY: ${KIE_API_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      
      # Storage
      STORAGE_TYPE: ${STORAGE_TYPE:-local}
      STORAGE_BASE_PATH: ${STORAGE_BASE_PATH:-/app/storage}
      
      # Worker config
      WORKER_CONCURRENCY: ${WORKER_CONCURRENCY:-5}
    volumes:
      - storage_data:/app/storage
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - neurocards
    # Масштабирование: docker-compose up --scale worker=10
    deploy:
      replicas: ${WORKER_REPLICAS:-3}
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 512M

networks:
  neurocards:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  storage_data:
=======
  # Background Worker (масштабируемый)
  worker:
    build: .
    command: python -m worker.worker
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - KIE_API_KEY=${KIE_API_KEY}
      - KIE_BASE_URL=${KIE_BASE_URL:-https://api.kie.ai}
      - KIE_CREATE_TASK_URL=${KIE_CREATE_TASK_URL:-https://api.kie.ai/api/v1/jobs/createTask}
      - KIE_RECORD_INFO_URL=${KIE_RECORD_INFO_URL:-https://api.kie.ai/api/v1/jobs/recordInfo}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_BASE_URL=${OPENAI_BASE_URL:-https://api.openai.com}
      - DATABASE_URL=${DATABASE_URL}
    network_mode: host
    restart: unless-stopped
    deploy:
      replicas: 5  # 🔥 5 воркеров для параллельной обработки
>>>>>>> 8f6520fa9541fa7c865a7c36d6faea7967bcf8fc
